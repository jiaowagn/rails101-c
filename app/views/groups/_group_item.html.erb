<tr>
  <td>#</td>
  <td><%= link_to(group.title, group_path(group)) %></td>
  <td><%= render_group_description(group) %></td>
  <td>
    <%= link_to("Edit", edit_group_path(group), class: "btn btn-default btn-sm") %>
    <%= link_to("Delete", group_path(group), method: :delete, class: "btn btn-default btn-sm", data: { confirm: "Are you sure?" }) %>
  </td>
</tr>
